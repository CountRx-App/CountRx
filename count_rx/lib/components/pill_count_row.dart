import 'package:flutter/material.dart';

class PillCountRow extends StatelessWidget {
  const PillCountRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
